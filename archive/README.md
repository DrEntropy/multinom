# Legacy Conda environments

These files were used before the project switched to [uv](https://docs.astral.sh/uv/) with PyMC 6.

- `environment.yml` — original conda-forge environment
- `working_env.yml` — Windows `libgcc` workaround (not needed on macOS or with PyMC 6 pip installs)

Current setup: see the project root `readme.md` and `pyproject.toml`.
